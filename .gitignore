# Settings (user explicitly excludes from repo)
settings.json

# Secrets
auth.json
.env

# Generated / runtime
npm/
sessions/
run-history.jsonl
models.json
mcp-cache.json
mcp-onboarding.json

# Extension runtime data
extensions/app_data/

# Extension local dependencies (directory-based extensions with their own package.json)
extensions/*/node_modules/
extensions/*/package-lock.json

# LSP cache
.pi-lens/

# OS files
.DS_Store
plan
pi-crash.log
